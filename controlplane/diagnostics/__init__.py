"""Component-level execution state, health, and failure localization.

Derived views over already-persisted runtime data -- no new tables. See
``component_state.py`` for the status/attribution vocabulary and
``report.py`` for the per-request assembler.
"""
